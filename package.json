{
  "main": "dist/main.js",
  "dependencies": {
    "typescript": "^5.5.4"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.3",
    "@vitest/browser": "^2.0.5",
    "ts-loader": "^9.5.1",
    "vitest": "^2.0.5",
    "webpack": "^5.94.0",
    "webpack-cli": "^5.1.4"
  },
  "scripts": {
    "build": "rm -r dist;webpack;cp -r src/datasets dist;",
    "test": "vitest",
    "release": "rm -r release;mkdir release;cp -r index.html release;cp -r style.css release;cp -r dist release;"
  }
}
